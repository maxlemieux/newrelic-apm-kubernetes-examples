apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-example.fullname" . }}
  labels:
    {{- include "java-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java-example.selectorLabels" . | nindent 4 }}
